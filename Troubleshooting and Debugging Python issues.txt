#Troubleshooting and Debugging Python issues

1. Connectivity issues (eg: DB connectivity, API Connectivity, etc)
2. Compile Time Errors(Syntax errors)
3. Run Time Errors(Exceptions)
4. Bugs in Python Code
5. Performance Issues

Read the error properly
Understand details provided along with errors
Troubleshoot root cause of the issue


Pandas for database connectivity but it's doesn't have its own ORM hence we can use SQL Alchemy internally

#performance tuning of Python Applications

import time

l=[1,2,3,4,5]

for i in l:
    print(f'Sleeping for {i}')
    time.sleep(i)

#Multiprocessing

import multiprocessing

#this doesn;t work
pool=multiprocessing.Pool(4)
pool.map(time.sleep,l)

def mySleep(n):
    print(f'Sleeping for {n}')
    time.sleep(n)
    

def main():
    l=[1,2,3,4,5,6]
    pool=multiprocessing.Pool(4)
    pool.map(mySleep,l)
    
    

if __name__=='__main__':
    main()

